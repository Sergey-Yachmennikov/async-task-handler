<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-5.0.xsd">

    <changeSet id="004-01-add-claim-token" author="erevan">
        <comment>Токен попытки: отличает повторный захват той же задачи тем же инстансом от предыдущей попытки</comment>
        <addColumn tableName="tasks">
            <column name="claim_token" type="VARCHAR(36)"/>
        </addColumn>
    </changeSet>

    <changeSet id="004-02-add-heartbeat" author="erevan">
        <comment>Отметка последней активности воркера — основа обнаружения зависших задач вместо started_at</comment>
        <addColumn tableName="tasks">
            <column name="heartbeat_at" type="TIMESTAMP WITH TIME ZONE"/>
        </addColumn>
    </changeSet>

    <changeSet id="004-03-stuck-tasks-heartbeat-index" author="erevan">
        <comment>Заменяет idx_tasks_stuck: поиск зависших задач теперь идёт по heartbeat_at</comment>
        <sqlFile path="sql/004-03-stuck-tasks-heartbeat-index.sql" relativeToChangelogFile="true"/>
        <rollback>
            DROP INDEX idx_tasks_stuck_heartbeat;
            CREATE INDEX idx_tasks_stuck ON tasks (started_at) WHERE status = 'IN_PROGRESS';
        </rollback>
    </changeSet>

</databaseChangeLog>
